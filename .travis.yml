language: node_js
node_js:
  - "7"

branches:
  only:
    - master

install:
  - npm install

before_script:
  - npm install -g angular-cli
  - npm install
  - ng build

cache:
  yarn: true
  directories:
  - node_modules

notifications:
  email:
    - alexandre.lerat@gmail.com
