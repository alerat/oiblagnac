language: node_js
node_js:
  - "7"

branches:
  only:
    - master

install:
  - npm install

before_script:
  - npm install -g @angular/cli

jobs:
  include:
  - stage: test
    script: npm run lint
  - stage: build
    script: npm run build

cache:
  yarn: true
  directories:
  - node_modules

notifications:
  email:
    - alexandre.lerat@gmail.com
    - r.olvera@laposte.net
